<h1>Dashboard#view</h1>
<p>Find me in app/views/dashboard/view.html.erb</p>
<% test = "yeehaw"
contracts = Contract.all%>

<a href="/contract/create">Make new promises</a>
<br/>
Your promises

<div>

  <% contracts.each do |contract| %>
    <div>
      <div display="inline-block"><%= contract.task %></div>
      <div display="inline-block"><%= contract.content %></div>
      <div>contract id: <%= contract.id %></div>
      <%= form_with url: contract_view_path, local: true do |form| %>

        <%= form.label :passphrase, '' %>
        <%= form.text_field :passphrase %>
        <input type="hidden" name="contractID" value="<%= contract.id %>"/>
        <%= form.submit %>
      <% end %>
    </div>
  <% end %>
</div>

Promises owed to you
